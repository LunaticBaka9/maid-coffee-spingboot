<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>

<mapper namespace="com.maid_coffee.mapper.AdminMapper">
    
    <select id="selectAllAdmins" resultType="com.maid_coffee.entity.Admin">
        select * from `user`
        <where>
                <if test="username != null">username like concat('%', #{username}, '%')</if>
                <if test="name != null">and name like concat('%', #{name}, '%')</if>
                and userusing != 0 and userType = "admin"
        </where>
        
    </select>

    <insert id="insert">
        insert into `user` (username, password, name, email, phone, userType, createTime)
        values(#{username}, #{password}, #{name}, #{email}, #{phone}, "admin", NOW())
    </insert>

    <!-- selectByUsername --> 

    <select id="selectByUsername">
        select * from `user` where username = #{username} and userType = "admin"
    </select>

    <update id="updateByUserId">
        update `user` set username=#{username}, password=#{password}, name=#{name}, email=#{email}, phone=#{phone}, userType=#{userType}
        where userId = #{userId}
    </update>

    <update id="deleteByUserId">
        update `user` set userusing = 0, deleteTime = NOW() 
        where userId = #{userId}
    </update>
    
    
</mapper>